# Generated reports (one dir per input batch, regenerated on every script run)
report-*/

# Script extraction target (auto-populated from input zips)
unpacked/

# OS / editor noise
Thumbs.db
.DS_Store
.vscode/
.idea/
*.swp
*.bak
*.tmp

# Local agent working data (per CLAUDE.md — private by default)
.Codex/

# Per-user Claude Code permissions / settings
.claude/settings.local.json

# Internal scratch dir used during git initialization
.gittmp/
